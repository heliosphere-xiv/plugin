query GetNewestVersionInfo($versionId: Int!) {
    getVersion(id: $versionId) {
        variant {
            versions(limit: 1) {
                id
                version
                groups {
                    name
                    options {
                        name
                    }
                }
            }
        }
    }
}
