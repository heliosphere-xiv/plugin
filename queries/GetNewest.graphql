query GetNewest($after: String, $before: String, $restricted: RestrictedInfoInput!) {
    packages(first: 3, after: $after, before: $before, restricted: $restricted) {
        nodes {
            ...PackageInfo
        }
    }
}
