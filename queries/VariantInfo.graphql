fragment VariantInfo on Variant {
    name

    package {
        name
    }

    versions(limit: 1) {
        id
        version
        changelog
        groups {
            standard {
                name
                options {
                    name
                }
            }

            imc {
                name
                options {
                    name
                }
            }
        }
    }
}
